name: Tests

on:
  push:
    branches: [ main, develop, 'feature/**' ]
  pull_request:
    branches: [ main, develop ]

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.12']

    steps:
    - uses: actions/checkout@v6

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"

    - name: Run linting
      run: |
        ruff check onshape_mcp tests

    - name: Run type checking
      run: |
        mypy onshape_mcp
      continue-on-error: true

    - name: Run tests with coverage
      # coverage packages/reports + fail_under all come from pyproject.toml ([tool.pytest.ini_options]
      # addopts and [tool.coverage.*]) — the single source of truth.
      run: |
        pytest -v

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v5
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-py${{ matrix.python-version }}
        fail_ci_if_error: false

    - name: Archive coverage reports
      uses: actions/upload-artifact@v6
      with:
        name: coverage-report-py${{ matrix.python-version }}
        path: htmlcov/

  coverage-check:
    runs-on: ubuntu-latest
    needs: test

    steps:
    - uses: actions/checkout@v6

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"

    - name: Check coverage threshold
      # fail_under lives in pyproject.toml [tool.coverage.report]; pytest-cov enforces it.
      run: |
        pytest --cov-report=term-missing
